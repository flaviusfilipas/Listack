// import {auth} from '../firebase'
const state = () => ({
    loggedIn: false
})
const actions = {
}
const mutations= {}
const getters= {}

export default {
    namespaced:true,
    state,
    actions,
    mutations,
    getters
}